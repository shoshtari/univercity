model:
  type: BiGRUTagger  # BiRNNTagger, BiLSTMTagger, BiGRUTagger
  embed_dim: 64
  hidden_dim: 64
  num_layers: 1
  dropout: 0.0

training:
  epochs: 10
  lr: 0.001
  batch_size: 32
  tag_key: ner_tags
  verbose: false

data:
  train_split: train
  val_split: valid

