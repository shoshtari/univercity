

# Using shiv
we want smaller images. shiv will take all files and put a huge pyz file instead. and in my tests, it could reduce 100MB of storage. the only problem was, I couldn't do it in two stages so that requirements were handled differently than code.
